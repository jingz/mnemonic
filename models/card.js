var Card = (function  (_super) {
    __extends(Card, _super);

    Card.table_name = "cards";

    function Card() {
        Card.__super__.constructor.apply(this, arguments);
    }

    Card.prototype.class = Card;
    return Card;
})(AR);
